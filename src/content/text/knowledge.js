import React from 'react';
import { H2 } from '../../components/UI/headings.styled';
import { P } from '../../components/UI/text.styled';
import {Copy} from '../../components/UI/containers.styled'

const knowledge = () => (
    <Copy>
        <H2> A knowledge root</H2>
        <P>We need a model that exists as in a single form. Not as a vast, scattered collection of snippets of understanding, unrelated analogies and notes living on your desktop.</P>
    </Copy>
);

export default knowledge;
