import React from 'react';
import { H2 } from '../styled/headings';
import { P } from '../styled/text';
import {Copy} from '../styled/containers'

const learn = () => (
    <Copy>
        <H2> The Right Way to Learn </H2>
        <P>Computers need 0s and 1s. We need programming languages to instruct computers. Our brains need story and analogy to understand them.</P>
    </Copy>
);

export default learn;
