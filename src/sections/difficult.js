import React from 'react';
import { H2 } from '../styled/headings';
import { P } from '../styled/text';
import {Copy} from '../styled/containers'

const difficult = () => (
    <Copy>
        <H2> Javascript is hard</H2>
        <P>I know it is, believe me. When you move beyond the basics, it's easy to be overcome by tutorial sinkholes and the demons of imposter syndrome. It doesn't take long before you find yourself learning so many new things - but without context.</P>
    </Copy>
);

export default difficult;
